#include<stdio.h>

int main()
{
    int a, b;
    scanf("%d%d", &a, &b);

    if (a == b)
    { 
        // what is the error
        printf("no are equal");
    }
    else {
        printf("no are not equal");
    }

    return 0;
}


